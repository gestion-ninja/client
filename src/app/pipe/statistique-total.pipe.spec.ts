import { StatistiqueTotalPipe } from './statistique-total.pipe';

describe('StatistiqueTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new StatistiqueTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
