import { AffiniteesPipe } from './affinitees.pipe';

describe('AffiniteesPipe', () => {
  it('create an instance', () => {
    const pipe = new AffiniteesPipe();
    expect(pipe).toBeTruthy();
  });
});
